		{
			"name": "fail2ban",
			"aptPackages": ["fail2ban"],
			"backup": [
				{
					"name": "fail2ban-configs",
					"type": "DIR",
					"path": "/etc/fail2ban/"
				}
			],
			"preRestore": [
				{
					"name": "Temporary stop service before copying configs",
					"command": "systemctl stop fail2ban"
				}
			],
			"postRestore": [
				{
					"name":"Start and enable fail2ban",
					"command": "systemctl daemon-reload; systemctl start fail2ban; systemctl enable --now fail2ban"
				}
			]

		}
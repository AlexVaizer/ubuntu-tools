		{
			"name": "nginx",
			"aptPackages": ["nginx"],
			"backup": [
				{
					"name": "nginx-configs",
					"type": "DIR",
					"path": "/etc/nginx/"
				}
			],
			"preRestore": [
				{
					"name": "Temporary stop service before copying configs",
					"command": "systemctl stop nginx"
				}
			],
			"postRestore": [
				{
					"name": "Remove default site",
					"command":"rm -f /etc/nginx/sites-enabled/default; rm -f /etc/nginx/sites-available/default"
				},
				{
					"name": "Start and Enable nginx",
					"command":"systemctl daemon-reload; systemctl start nginx; systemctl enable --now nginx"
				}
			]
		}